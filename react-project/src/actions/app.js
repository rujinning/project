import * as actionType from './actionTypes';
export const getProductListData = (params) => (
    {
        type: actionType.HELLO_WORLD,
        params
    }
);
export const postordertag = (params) => (
    {
        type: actionType.POSTORDERTAG,
        params
    }
);